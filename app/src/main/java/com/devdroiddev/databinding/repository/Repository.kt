package com.devdroiddev.databinding.repository
import com.devdroiddev.databinding.api.RetrofitInstance
import com.devdroiddev.databinding.model.PostModel
import retrofit2.Response

class Repository (){
    /*suspend fun pushPost(userId: Int, id: Int, title: String, body: String) : Response<PostModel> {
        return return RetrofitInstance.api.pushPost(userId, id, title, body)*/
    }
